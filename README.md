# environmental-analytics
End-to-end machine learning project that predicts Air Quality Index (AQI) using environmental data. The solution covers the complete ML lifecycle including data cleaning, EDA, feature engineering, training and comparing multiple regression models, hyperparameter tuning, model selection, and local deployment using Streamlit.
